import JoinAuthForm from "./_components/JoinAuthForm";

export default async function Auth() {
  return (
    <main className="flex h-screen items-center justify-center">
      <JoinAuthForm />
    </main>
  );
}
